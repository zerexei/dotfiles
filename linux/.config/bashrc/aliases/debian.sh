#!/bin/bash

# Debian / Ubuntu — apt
alias apti='sudo apt install'      # install
alias aptr='sudo apt remove'       # remove
alias aptu='sudo apt update && sudo apt upgrade'  # update all
alias apts='apt search'            # search
alias aptsi='apt show'             # package info
alias aptli='apt list --installed' # list installed
alias aptac='sudo apt autoremove && sudo apt autoclean'  # cleanup
